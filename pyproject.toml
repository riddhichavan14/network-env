[project]
name = "network-env"
version = "0.1.0"
description = "Network Environment RL API"
authors = [
    {name = "Riddhi"}
]
dependencies = [
    "flask",
    "requests",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"

[tool.setuptools]
packages = ["."]
